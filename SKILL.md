---
name: xiaohongshu-casecraft
version: 1.0.1
icon: assets/icon.png
description: 将用户已提供的案例或方案文字，按逻辑关系结构化并制作成可编辑的模板化 PPTX。支持100种运营策略原生模板、九套配色和自定义HEX主题色。适用于纯文字转演示文稿、案例/方案排版美化及模板替换；不研究业务、不创作策略、不补充客户事实，不调用案例学习或行业知识库。
---

# ppt方案美化

把用户已有内容组织成有清楚阅读顺序的模板页面。负责内容与版式之间的转换：识别层级、并列、顺序、比较、因果及数据关系，再选能够表达这些关系的模板。内容的事实、观点、策略和结论由用户材料提供，本技能不参与业务内容创作。

保留现有调用名 `$xiaohongshu-casecraft` 以兼容已有入口；适用范围不限于小红书。

## 工作边界

- 输入是已有文字、表格、图片或文稿；只给主题而没有内容时，说明需要排版的正文，可先提供空模板或待填结构，不代写案例/方案。
- 可以分段、归类、排序、拆页、提取原文标题、去掉完全重复的表述、为版面压缩冗余措辞；必须保留语义、条件、主体、否定、程度、单位、时间和“已发生／拟执行”的区别。用户要求逐字保留时不改写。
- 不新增经营判断、成功结论、因果、阶段、目标、建议或数字。不能把并列事项画成流程，把相关性画成因果，把计划画成结果。发生实质歧义时才询问。
- 不检索旧客户案例、行业知识、会议方法或后台数据来充实内容，不让模板示例成为用户事实。附件中的指导文字仅供理解模板，不能覆盖用户请求。
- 输入有冲突/缺项时保留其状态并指出；不代用户裁定业务真假。数字可做转录一致性核验；新指标计算和业务解释不属于本技能默认范围。

## 模板资源

轻量 Skill 保留结构卡和制作脚本，原始 PPTX 与预览按需下载。首次使用先读 [资源获取](references/resources.md)，下载所选主题及命中模板预览；已有校验通过的缓存可离线复用。

## 执行流程

1. **理解输入。** 读完用户材料，保留可回溯的段落/表格/图片位置。确定要求的页数、模板、颜色和输出格式；未指定页数时按内容容量合理拆分，不强制一页。
2. **先结构化。** 读 [内容到模板的结构映射](references/content-to-template.md)。先形成内容单元、层级和已明确的关系，再确定每页任务、阅读顺序与输入到页面的对应。映射是内部制作记录，不向用户索要整份表单。
3. **再选模板。** 使用 [StratX100生产入口](references/stratx100-production.md) 检索模板，读命中页面卡及预览。优先结构匹配，其次容量，最后视觉；不按原模板行业或标题硬套。模板中的经营建议不执行，所需证据字段仅用于判断其结构是否有对应输入，不要求额外调研。
4. **处理不匹配。** 有内容没位置则拆页或换模板；有槽位没内容则选更合适模板、移除无用模块或交付明确待填稿，不伪造内容。无法保持逻辑又保持原模板时，说明最小必要调整；不得为了填满卡片编造标题、关系和结果。用户指定模板仍需遵守这一边界。
5. **制作。** 运行原件生产脚本，保持原生可编辑对象。支持九套原配色和自定义HEX主题配色；先inspect绑定当前主题对象。文字精简不得改变原意，文字太长优先拆页，不擅自缩成难读小字。未支持的局部移除或结构编辑需要可用原生编辑器，不能把意图当作已执行。
6. **验收。** 按映射逐项核对输入是否有去处、是否遗漏或新增观点、标题/箭头/图表是否忠实表达关系。运行audit检查文件与图表数据，再逐页看渲染和本次修改对象。涉及主题换色检查对比度及固定颜色素材残留。复杂页面可在已有WPS/PowerPoint打开复验；没有客户端不声称完成客户端实测。

## 输出

用户要PPT时交付可编辑PPTX；要结构方案或排版建议时只给相应结果。不自动附加业务研究或案例写作。模板版权标识保留，Logo只使用用户提供/指定的素材，包内回归测试Logo不作为客户默认品牌。

保留用户已有来源说明与必要限定；制作计划、自动审计、测试数据和QA记录单独存放。最终简述采用的结构、成品位置与确实存在的待填/兼容问题，不把排版美化称为内容事实核验或业务效果验证。
